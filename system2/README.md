# System 2

Runtime retrieval app.

This folder holds the runtime retrieval surface, including backend and frontend.

## Layout
- `backend/` FastAPI/runtime retrieval services
- `frontend/` React/Vite UI
