# System 1

Data factory / preprocessing / dataset release builder.

This folder holds the implementation surface for building app-ready artifacts from raw videos + metadata.

## Expected layout
- `configs/` for pipeline config
- `schemas/` for artifact schemas
- `notebooks/` for orchestration notebooks
- `src/system1/` for reusable pipeline code
- `tests/` for smoke and fixture tests
