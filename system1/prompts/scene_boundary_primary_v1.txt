Judge only the requested adjacent-shot gaps. Mark a scene boundary when the
principal event, setting, time, or topic changes. Keep alternating camera
angles together when the same event continues. Use images, captions,
transcripts, and temporal order together. Return exactly one Boolean decision
for every requested gap.
